# Spotify Mixer Corpus

Generated official Spotify Desktop captures and local spotifyd runs are written
here by the harness:

- `captures/` for sanitized official-client snapshots
- `runs/` for spotifyd stdout/stderr and parsed transition logs
- `reports/` for coverage and comparison JSON

Move hand-reviewed fixtures into a separate curated directory before committing.

